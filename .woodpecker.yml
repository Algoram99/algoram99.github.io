steps:
  build:
    image: alpine:3.20
    commands:
      - 'echo "Woodpecker CI pipeline running on Algoram99/algoram99.github.io"'
      - 'echo "Event ${CI_PIPELINE_EVENT} branch ${CI_COMMIT_BRANCH} commit ${CI_COMMIT_SHA}"'
      - 'ls -la'
      - 'echo "---"'
      - 'head -5 index.html'

labels:
  host: algoram88-mac
